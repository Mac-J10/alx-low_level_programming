#include <stdio>[D.h[>
/**
 *main - print if the number is positive, zero or negative
 *
 *Description: using the main fnction[D[D[D[D[D[Dunction
 *this program prints "Programming is positive, zero or negtive
 Return: 0
[A[C*Return: 0
 */

int main(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++);
{
	putchar(ch);
}
for (ch = 'A'; ch < 'Z'; ch++);[D[D[D[D[D[D[D[D[D[D[D[D= 'Z'; ch++);
{
	putchar(ch);
}
putchar('\n');
return (0);
}
