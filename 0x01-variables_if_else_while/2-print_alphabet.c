#include <stdio.h>
/**
*main - print if the number i positive, zero or negative
*
*Description: using the main function
*this program prints "Programming is positive, zero or negative
*Return: 0
*/
int main()[Dvoid)
{
	char c;
	for (ch = 'a' ; ch <= 'z' ; ch ++);
[D[D[D[D[D[D{
		putchar(ch);
	}
	putchar('\n');
return (0);
}
